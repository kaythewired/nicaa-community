packages:
  - "."

overrides:
  postcss: 8.5.23
  sharp: 0.35.0

# Required by the Vinext/Cloudflare build toolchain. Keeping the approval list
# explicit lets the hosted installer run only the binary setup scripts it needs.
allowBuilds:
  esbuild: true
  workerd: true
